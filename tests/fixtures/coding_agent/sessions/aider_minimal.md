# aider chat started at 2026-04-29T13:00:00Z

#### Please add a docstring to `src/utils.py`.

I'll add a module-level docstring describing the helpers.

src/utils.py
```
"""Small grab-bag of helpers used across the project."""

def add(a, b):
    return a + b
```

The change introduces a top-level module docstring and keeps the
existing `add` helper unchanged.

#### Thanks. Now run pytest.

I cannot execute commands directly, but you can run:

```
pytest -q
```

Let me know the output if anything fails.
