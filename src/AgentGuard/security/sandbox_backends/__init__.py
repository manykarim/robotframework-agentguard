"""Real sandbox backend implementations (Phase 2).

The Phase 1 ``security/sandbox.py`` only probed for backend availability; this
package adds actual ``run_in_sandbox`` execution paths per ADR-013.
"""
